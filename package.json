{
  "name": "url-shortener-monorepo",
  "version": "1.0.0",
  "description": "Full-stack URL Shortener with high-performance analytics, caching, and rate limiting",
  "scripts": {
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start:backend": "npm start --prefix server",
    "start:frontend": "npm run dev --prefix client"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
